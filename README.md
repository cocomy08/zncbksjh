# 七七手机 (QiQi Phone)

这是一个基于 Web 技术构建的高保真 iOS 手机界面模拟器，专为沉浸式角色扮演与情感互动设计。项目包含一个纯前端 PWA 应用以及一个配套的音乐服务后端。

## 📱 项目简介

"七七手机" 模拟了一个虚拟角色的手机界面。用户可以在为了 Web 优化的界面中体验各种 App 功能，包括模拟聊天、查看朋友圈（动态）、听音乐、购物以及访问私密空间（小窝）。

项目特别针对 iOS PWA（渐进式 Web 应用）进行了深度优化，解决了橡皮筋回弹、安全区适配、内存管理等关键体验问题。

## ✨ 核心功能

- **高保真 iOS UI**: 完美复刻 iOS 状态栏、控制中心、动态岛（Dynamic Island）及应用图标交互。
- **模拟应用生态**:
  - **讯息 (Messages)**: 与 AI 角色进行实时对话。
  - **世界书 (Worldbook)**: 查看角色设定与背景故事。
  - **音乐 (Music)**: "一起听"功能，支持同步听歌与情感反馈（需启动后端）。
  - **哇呜 (Wowo)**: 情侣空间/私密空间，包含心情日记与私密贴纸。
  - **动态 (Gacha)**: 模拟朋友圈/社交动态流。
  - **购物 (Shop)**: 模拟电商购物体验。
- **PWA 深度优化**:
  - 支持添加到主屏幕，全屏沉浸运行。
  - 内置 iOS 内存优化与自动清理机制。
  - 自定义安全区（Safe Area）适配与触摸手势拦截。

## 📂 目录结构

```
urtve-main/
├── index.html              # 前端入口文件
├── main.js                 # 核心逻辑入口（包含PWA优化、全局状态管理）
├── sw.js                   # Service Worker（离线缓存与更新）
├── manifest.json           # PWA 配置文件
├── src/
│   ├── scripts/            # 各功能模块脚本
│   │   ├── boomboom.js     # 心跳/震动功能
│   │   ├── littleworld.js  # 小世界/私密聊天逻辑
│   │   ├── music-player.js # 音乐播放器逻辑
│   │   ├── wowo.js         # 哇呜/小窝逻辑
│   │   └── ...
│   └── styles/             # 模块化样式文件
├── music-server/           # 音乐功能后端服务 (Node.js)
├── assets/                 # 静态资源目录
└── prototype/              # 原型设计文件
```

## 🚀 快速开始

本项目包含前端和后端两部分。

### 1. 前端启动

前端为纯静态网页，可以使用任何静态文件服务器启动。

**方式 A: 使用 Live Server (推荐)**
如果你使用 VS Code，安装 "Live Server" 插件，右键 `index.html` 选择 "Open with Live Server"。

**方式 B: Python 简易服务器**

```bash
python -m http.server 8000
# 访问 http://localhost:8000
```

### 2. 后端启动 (音乐服务)

如果需要使用音乐播放与搜索功能，需要启动 `music-server`。

```bash
cd music-server
npm install
npm start
# 服务默认运行在 3000 端口
```

> **注意**: 部分音乐 API 可能需要配置额外的环境变量或 Netease Cloud Music 账号 Cookie，详见 `music-server` 目录下的说明。

## 🛠️ 技术栈

- **前端**:
  - HTML5 / CSS3 (Vanilla + Tailwind CSS)
  - JavaScript (ES6+)
  - IndexedDB (本地数据存储)
  - PWA (Service Worker, Manifest)
- **后端**:
  - Node.js
  - Express
  - NeteaseCloudMusicApi (网易云音乐 API)

## ⚠️ 注意事项

- **iOS 体验**: 推荐使用 Safari 将网页"添加到主屏幕"以获得最佳体验。
- **调试**: 默认关闭了 Debug 模式。如需调试，请在 `main.js` 中将 `window.DEBUG_MODE` 设置为 `true`。

## 📄 许可证

Private / Unknown
